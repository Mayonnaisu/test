name: Sync Feature Branch
on:
  push:
    branches: [ main ]
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Merge main into feature
        run: |
          git fetch origin
          git checkout second
          git rebase origin/main --no-edit
          git push -f origin second