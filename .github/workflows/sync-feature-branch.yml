name: Sync Feature Branch
on:
  push:
    branches: [ main ]
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Merge main into feature
        run: |
          git fetch origin
          git checkout second fetch-depth: 0
          git rebase origin/main
          git push -f origin second